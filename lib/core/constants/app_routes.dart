class AppRoutes {
  AppRoutes._();

  static const String home = '/';

  // El resto de la navegación usa MaterialPageRoute directo (ver AppShell) —
  // no hay rutas nombradas por pantalla todavía porque no hace falta deep
  // linking ni back-stack complejo en esta etapa.
}
